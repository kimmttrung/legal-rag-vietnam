# Dependencies CHỈ cho app demo (app/) — nhẹ hơn requirements.txt của đường thi.
# App KHÔNG nạp LLM local (gọi API) nên bỏ transformers*/accelerate/bitsandbytes trực tiếp
# (transformers vẫn được kéo về gián tiếp qua sentence-transformers).
qdrant-client>=1.9.0
rank_bm25>=0.2.2
sentence-transformers>=2.7.0
underthesea>=6.8.0
openai>=1.30.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
numpy>=1.26.0
